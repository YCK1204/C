#include <stdio.h>

int n, a[10001];

int main(){
    scanf("%d", &n);
    int t;
    for(int i = 0; i < n; i++){
        scanf("%d", &t);
        a[t]++;
    }
    
    for(int i = 1; i < 10001; i++){
        t = a[i];
        while(t--){
            printf("%d\n", i);
        }
    }
    return 0;
}
